apiVersion: v1
kind: ConfigMap
metadata:
  name: portal-c-config-staging
  namespace: exam-3
data:
  config.json: |
    {
      "app_name": "portal-c",
      "subtitle": "Staging - GitOps via Argo CD",
      "owner": "Hans"
    }
