apiVersion: v1
kind: Service
metadata:
  name: portal-c-staging
  namespace: exam-3
spec:
  type: NodePort
  selector:
    app: portal-c
    env: staging
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30083
