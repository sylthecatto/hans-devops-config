apiVersion: v1
kind: Service
metadata:
  name: portal-c-production
  namespace: exam-3
spec:
  type: NodePort
  selector:
    app: portal-c
    env: production
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30183
